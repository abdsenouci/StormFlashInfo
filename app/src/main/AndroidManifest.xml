<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.abdallah.stormflashinfo">

    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo_sfi"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo_sfi_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity
            android:name=".launcherActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".menuActivity"
            android:screenOrientation="portrait" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity android:name=".BonPlanActivity" />
        <activity android:name=".LieuActivity" />
        <activity android:name=".NouveauBonPlanActivity" />
        <activity android:name=".categorieActivity" />
        <activity android:name=".ListeBonPlansActivity" />
        <activity android:name=".ListeLieuxActivity"></activity>
    </application>

</manifest>